<%= form_for(@api_key, url: {action: "create"}, html: { class: "pure-form pure-form-stacked" }) do |f| %>
  <% if @api_key.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@api_key.errors.count, "error") %> prohibited this api_key from being saved:</h2>

      <ul>
      <% @api_key.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <%= f.label "App name" %><br>
  <%= f.text_field :name, placeholder: 'Your application name' %>

  <div class="actions">
    <%= f.submit "Create API Key", class: "pure-button pure-button-primary" %>
  </div>
<% end %>
